// import type { PageServerLoad } from './$types';
// import { getPins } from './api/pins/+server';

// export const load = (async ({ fetch }) => {
// 	const pins = await getPins();
// 	return { pins };
// }) satisfies PageServerLoad;
